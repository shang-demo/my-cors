{
  "name": "my-cors",
  "version": "0.1.0",
  "engines": {
    "node": ">=8.0.0"
  },
  "scripts": {
    "dev": "node-dev app.js",
    "start": "NODE_ENV=leancloud pm2 start ./app.js --no-daemon",
    "test": "mocha --recursive --timeout 10000 --harmony --bail test"
  },
  "dependencies": {
    "bufferhelper": "^0.2.1",
    "cors": "^2.8.4",
    "eruda": "^1.2.6",
    "http-proxy": "^1.16.2",
    "iconv-lite": "^0.4.19",
    "pm2": "^2.7.1",
    "proxy-agent": "^2.1.0",
    "require-directory": "^2.1.1",
    "requires-port": "^1.0.0"
  },
  "devDependencies": {}
}
